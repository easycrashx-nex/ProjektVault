services:
  arcane-vault:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arcane-vault
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./backend/data:/app/backend/data
